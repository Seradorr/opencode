# OpenCode Global Rules

- Reply in Turkish when the user writes Turkish. Keep code, commands, and identifiers in English.
- Work from the real filesystem and current repo state. Read affected files before editing them.
- Prefer minimal, targeted changes over broad rewrites. Do not refactor unrelated code unless the user asks for it.
- Load the matching domain skill before making implementation or planning decisions: `fpga`, `c-systems`, `vitis`, `python`, `dotnet`, `document`, `ai-toolkit`.
- Validate with the cheapest relevant check after meaningful edits.
- Treat this installation as closed-network by default. Do not assume public internet or SaaS services are available unless the user explicitly says so.
